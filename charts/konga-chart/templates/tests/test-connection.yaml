apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "konga-install.fullname" . }}-test-connection"
  labels:
    {{- include "konga-install.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "konga-install.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
