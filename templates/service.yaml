apiVersion: v1
kind: Service
metadata:
  name: {{ include "konga-install.fullname" . }}
  labels:
    {{- include "konga-install.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konga-install.selectorLabels" . | nindent 4 }}
